[package]
name = "albion-db"
version = "0.1.0"
edition = "2024"
description = "PostgreSQL connection and migrations for AlbionMC"

[dependencies]
sqlx = { version = "0.8", default-features = false, features = ["runtime-tokio", "postgres", "uuid"] }
toml = "0.9"
log = "0.4"
